import React from 'react';
import * as MediaLibrary from 'expo-media-library';

import {
    Text,
    View,
    Image,
    ImageBackground,
    StyleSheet,
    ScrollView
} from 'react-native';

import ImageNavigate from './ImageNavigate';
import Touchable from './Touchable';
import getFilesFromMedia from '../util/GetFilesFromMedia';


const GalleryGenerator = () => {
    return (
        <View>

        </View>
    );
}
